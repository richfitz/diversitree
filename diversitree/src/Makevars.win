# -*-makefile-*-
PKG_CPPFLAGS=-I./cvodes/include -Icvodes -I.
PKG_CFLAGS=
PKG_LIBS=${LAPACK_LIBS} ${BLAS_LIBS} ${FLIBS} -lfftw3

## On my windows machine:
# PKG_CPPFLAGS=-I./cvodes/include -Icvodes -I. -Ic:/fftw
# PKG_CFLAGS=
# PKG_LIBS=${LAPACK_LIBS} ${BLAS_LIBS} ${FLIBS} -Lc:/fftw -lfftw3-3
