#!/bin/sh

## Simple configure script for windows.
if [ -f $LIB_FFTW/fftw3.h ]; then
    echo '#define HAVE_FFTW3_H 1' > src/config.h
    echo "Using fftw for QuaSSE FFT calculations"    
else
    echo '' > src/config.h
    echo "WARNING: No fftw found - QuaSSE/fftC will not be available"
fi
